import { TableScanPipe } from './table-scan.pipe';

describe('TableScanPipe', () => {
  it('create an instance', () => {
    const pipe = new TableScanPipe();
    expect(pipe).toBeTruthy();
  });
});
